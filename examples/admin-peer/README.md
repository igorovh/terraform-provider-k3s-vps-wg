# Admin Peer

Generates a WireGuard peer config for one workstation. The provider does not install it locally.

For teams or pipelines, prefer the `admin_peers` map with developer-provided public keys.
