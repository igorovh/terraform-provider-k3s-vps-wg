# Admin Peers

Configures multiple WireGuard admin peers for developers and CI/CD pipelines.

Peers with `public_key` do not store private keys in Terraform state. Peers without `public_key` are generated by the provider and returned in `admin_wireguard_configs`.

For long-lived team access, prefer explicit `wg_ip` values so adding another peer does not change addresses.
